node_modules
.env
./backend/.env

# build output
dist
build

# local tooling / verification artifacts
.playwright-mcp/
verify-*.png

# credentials / keys — never commit
token
token.pub
*.pem
id_rsa*
*.key
